#include <stdlib.h>
#include <stdio.h>

/**
 * main : Entry point
 *
 * Reture 0: (Always Success)
 *
 **/

int main (void)
{
	char alph;
	       alph = 'a';
		while (alph <= 'z')
		{
			putchar(alph);
			
			alph ++;
	
		}

		putchar('\n');

		return(0);
}
